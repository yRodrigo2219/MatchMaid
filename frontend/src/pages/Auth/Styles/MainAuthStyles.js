import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonAuth:{     
        marginTop : 30,
        alignItems :'center',
        paddingTop : 10,
        paddingLeft : -30
        }
});